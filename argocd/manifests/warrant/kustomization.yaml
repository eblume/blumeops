---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: warrant
resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - ingress-tailscale.yaml
images:
  - name: registry.ops.eblu.me/blumeops/warrant
    # Placeholder — set to the real tag after the first Build Container
    # dispatch of `warrant` (v0.1.0-<sha>-nix).
    newTag: UNBUILT
