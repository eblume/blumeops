apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: alloy

resources:
  - namespace.yaml
  - rbac.yaml
  - daemonset.yaml

images:
  - name: registry.ops.eblu.me/blumeops/alloy
    newTag: v1.19.2-ed1df80-nix

configMapGenerator:
  - name: alloy-config
    files:
      - config.alloy
