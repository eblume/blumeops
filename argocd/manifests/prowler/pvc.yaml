apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowler-reports
  namespace: prowler
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: prowler-reports-nfs-pv
  resources:
    requests:
      storage: 10Gi
