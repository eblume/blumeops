apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: alloy

resources:
  - rbac.yaml
  - daemonset.yaml

images:
  - name: registry.ops.eblu.me/blumeops/alloy
    newTag: v1.19.2-45387b3-nix

configMapGenerator:
  - name: alloy-tracing-config
    files:
      - config.alloy
