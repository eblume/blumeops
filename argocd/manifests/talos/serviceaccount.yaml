# Restricted ServiceAccount: NO Kubernetes API access —
# no Role/RoleBinding, token not mounted. The pod's only outward reach is
# the tag:agent tailnet fence. See [[agent-containerization]].
apiVersion: v1
kind: ServiceAccount
metadata:
  name: talos
  namespace: talos
automountServiceAccountToken: false
