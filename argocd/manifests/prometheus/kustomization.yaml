apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - statefulset.yaml
  - service.yaml
  - ingress-tailscale.yaml

images:
  - name: registry.ops.eblu.me/blumeops/prometheus
    newTag: v3.10.0-0d27797

configMapGenerator:
  - name: prometheus-config
    files:
      - prometheus.yml
