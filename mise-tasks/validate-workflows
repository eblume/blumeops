#!/usr/bin/env bash
#MISE description="[human] Validate Forgejo workflow files against runner schema"

set -euo pipefail

"$(dirname "$0")/_require" docker

dagger call --progress=plain validate-workflows --src=.
