#!/usr/bin/env bash
#MISE description="[human] Deploy the Fly.io public proxy"

set -euo pipefail

export FLY_API_TOKEN
FLY_API_TOKEN="$(op read 'op://blumeops/fly.io admin/add more/deploy-token')"

cd "$(dirname "$0")/../fly"
fly deploy "$@"
