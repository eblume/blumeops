Reviewed the GitOps explanation (`docs/explanation/why-gitops.md`): corrected the layered-tools table — the ansible layer also manages ringtail's NixOS rebuild and sifaka exporters, and the pulumi layer manages tailnet auth keys alongside ACLs and tags — and noted that ArgoCD syncs most apps automatically with a few deliberate manual exceptions. Stamped `last-reviewed: 2026-09-07`.
