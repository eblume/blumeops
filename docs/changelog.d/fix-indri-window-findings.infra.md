indri: the flake now sets `power.sleep.computer = "never"` and restores `/etc/shells`; the provision runbook drops the "no-write rehearsal" (nix-darwin's `activate` ignores `checkActivation` — every call is a real activation), moves Determinate's step 0 to the console (Tahoe TCC-protects `/etc/fstab` against ssh sessions), and records the reboot test's answer: Background Task Management disallows `org.nixos.activate-system`, so `/run/current-system` never survives a reboot.
