indri: fail at the top of the indri play when the nix-darwin-provided mise binary is below `indri_mise_min_version` (2025.11.0; prek entered the mise registry in 2025.8.11, which the global mise config and `mise prune` need). The [[indri]] card now documents where indri's toolchain comes from, how to locate the nix-darwin source of truth, and the runbook for bumping mise. Tracked in eblume/blumeops#1066.
